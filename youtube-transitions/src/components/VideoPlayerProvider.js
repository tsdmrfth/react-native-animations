import React from 'react';
import VideoPlayerContext from './VideoPlayerContext';

export default class VideoPlayerProvider extends React.Component {

    render() {
        return (
            <VideoPlayerContext.Provider>

            </VideoPlayerContext.Provider>
        )
    }

}